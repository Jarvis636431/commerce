# Commerce 学习路线图

这个项目以模块化单体为主线，目标不是堆砌中间件，而是通过电商业务理解 Java 后端常见的数据、缓存、消息、安全、搜索、存储和可观测性方案。

## 已完成

- 商品、SKU 与库存
- 库存预占、确认、释放和并发控制
- 订单、成交快照与地址快照
- 支付、超时关单和库存补偿
- 用户、地址、Spring Security 与双 Token
- Redis 购物车与商品详情缓存
- RabbitMQ、延迟消息、重试和死信队列
- Transactional Outbox 与至少一次投递
- Prometheus、Loki、Alloy、Grafana 与 Alertmanager
- 整单和部分金额退款
- 退款创建幂等、回调幂等、额度锁定和异步渠道提交

## 下一阶段：搜索与对象存储

### 1. Elasticsearch 商品搜索

目标：不再使用数据库 `LIKE` 承担复杂商品检索。

- [x] 使用 Spring Data Elasticsearch 接入 Elasticsearch
- [x] 设计商品搜索文档和 Index Mapping
- [x] 支持名称、描述、SKU 的全文检索
- [x] 支持价格区间、上下架状态过滤和更多排序方式
- [x] 学习倒排索引、相关性评分和高亮
- [ ] 接入 IK 中文分词插件，创建版本化新索引并使用 alias 原子切换
- [x] 使用 Outbox/RabbitMQ 可靠同步 PostgreSQL 商品变更到 ES
- [x] 实现事务提交后增量更新和管理员全量重建
- [x] 明确 PostgreSQL 是事实源，ES 是可以重建的查询副本
- [x] 增加索引同步失败指标、日志和告警

### 2. MinIO / S3 对象存储

目标：商品图片不保存到数据库，也不依赖应用服务器本地磁盘。

- [x] 使用 MinIO 在本地模拟 S3 对象存储
- [x] 实现商品图片上传、查询和删除
- [x] 数据库只保存对象 Key、类型、大小和业务归属
- [x] 使用预签名 URL 让客户端直传和下载
- [x] 校验 MIME、扩展名、文件大小和对象 Key
- [x] 学习 Bucket、Object、ETag 和访问策略
- [x] 支持商品主图、图片排序，并同步到商品详情和 ES
- [x] 定时清理过期 PENDING 上传
- [x] 使用 Outbox/RabbitMQ 异步清理删除图片和商品孤儿对象
- [ ] 配置对象生命周期规则
- [x] 处理数据库记录与对象存储之间的一致性和垃圾文件清理
- 后续可无缝迁移到 AWS S3、阿里云 OSS 或腾讯云 COS

推荐顺序是先 Elasticsearch、后 MinIO。搜索可以直接复用商品模块、Outbox 和 MQ，把“数据库事实源 + 异构索引最终一致性”串起来；对象存储随后作为独立且边界清晰的文件能力接入。

## 下一阶段：优惠券与促销

目标：先完成正确、可验证的数据库版本，再逐步引入 Redis 和 MQ 优化高并发领取；最终与订单、支付和退款形成业务闭环。

### 1. 数据库版优惠券

- [ ] 设计 `CouponTemplate`、`UserCoupon` 和优惠规则模型
- [ ] 支持满减券与折扣券（含最低消费和最高优惠）
- [ ] 实现模板 `DRAFT → ACTIVE → PAUSED → ENDED` 状态机
- [ ] 实现用户券 `AVAILABLE → LOCKED → USED / EXPIRED` 状态机
- [ ] 支持发行总量、每人限领、领取时间和有效期限制
- [ ] 使用数据库条件更新、唯一约束和幂等键防止超发、超领及重复领取

### 2. 接入订单生命周期

- [ ] 下单时校验券归属、状态、有效期、门槛和适用范围
- [ ] 在创建订单的本地事务中同时预占库存并锁定优惠券
- [ ] 保存优惠规则、优惠金额和用户券 ID 快照
- [ ] 支付成功时幂等核销 `LOCKED → USED`
- [ ] 订单取消或支付超时时幂等释放 `LOCKED → AVAILABLE`
- [ ] 处理同一张券被两个并发订单使用的冲突

### 3. 优惠计算与金额分摊

- [ ] 明确商品原价、商品优惠、订单优惠、运费和实付金额的计算顺序
- [ ] 按订单项实付金额比例分摊订单级优惠
- [ ] 使用整数分或统一舍入策略计算，最后一项承担尾差
- [ ] 在订单项中保存原价、优惠分摊和实付金额快照
- [ ] 为后续按订单项退款提供准确的可退金额

### 4. 并发测试与 Redis Lua

- [ ] 编写并发领取测试，观察数据库锁竞争、正确性和吞吐量
- [ ] 使用 Redis Lua 原子完成库存检查、个人限领检查和资格预扣
- [ ] Redis Cluster Key 使用相同 Hash Tag，确保脚本涉及的 Key 位于同一 Slot
- [ ] 控制 Lua 脚本复杂度和执行时间，避免阻塞 Redis
- [ ] 对比纯 PostgreSQL 与 Redis Lua 方案的吞吐和延迟

### 5. MQ 异步落库与一致性

- [ ] Redis 预扣成功后通过 RabbitMQ 异步创建 `UserCoupon`
- [ ] 使用领取请求 ID 和数据库唯一约束保证重复消费幂等
- [ ] 消费失败执行有限重试，耗尽后进入优惠券 DLQ
- [ ] 设计 Redis 预扣成功但消息发送或数据库写入失败的补偿
- [ ] 定时对账 Redis 发放量、模板库存和 PostgreSQL 用户券
- [ ] 提供异常记录、人工修复、指标、日志和告警

### 优惠券模块完成标准

以下链路全部完成后，可以认为当前学习项目的优惠券核心模块比较完善：

```text
模板创建与启停
→ 数据库正确领取与限领
→ 下单锁券并保存优惠快照
→ 支付核销 / 取消释放
→ 优惠金额按订单项分摊
→ Redis Lua 高并发预扣
→ RabbitMQ 异步幂等落库
→ Redis / PostgreSQL 对账补偿
→ 按订单项正确退款
```

生产级促销平台仍可继续扩展商品券、品类券、店铺券、优惠叠加与互斥、自动择优、活动人群、风控、防刷、成本归属和运营报表；这些不作为当前阶段的完成门槛。

## 退款模块后续增强

这些能力已经进入 Roadmap，当前不阻塞搜索和对象存储学习：

### 按订单项退款与金额分摊

- 新增 `RefundItem` 退款明细
- 按订单项成交单价和数量计算退款金额
- 控制累计可退数量不超过购买数量
- 在优惠券模块完成后，根据商品实付金额快照退款
- 处理优惠分摊的四舍五入和最后一项尾差
- 处理运费退款、订单级优惠和多商家分摊

### DLQ 管理与重放

- 将 RabbitMQ 死信持久化为 `DeadLetterRecord`
- 提供管理员分页查询和失败原因查看接口
- 记录处理人、处理备注和审计时间
- 管理员重放时创建新 Outbox 事件，不直接操作 RabbitMQ
- 防止重复重放，并记录原事件与新事件的关联
- 增加 DLQ 深度、持久化失败和重放失败告警

### 可靠性补充

- 扫描长时间停留在 `PROCESSING` 的退款并主动查询渠道
- 人工审核和退款审批
- 退款账务流水与财务对账
- 售后、退货、换货和退货物流

完成订单项退款、金额分摊、DLQ 管理和超时补偿后，可以认为退款模块的核心业务与可靠性闭环较完整；真实资金生产系统仍需要账务、对账、审核和风控。

## 工程化验收阶段

业务模块完成后进入统一验收，不再继续无边界堆叠业务功能。完成本阶段后，项目应达到“生产化风格的模块化单体学习项目”标准。

### 1. OpenAPI 与接口契约

- [ ] 接入 Springdoc OpenAPI / Swagger UI
- [ ] 为核心接口补充请求、响应和错误示例
- [ ] 统一分页、Problem Detail、幂等键和鉴权说明
- [ ] 标注 USER、ADMIN 和公开接口的权限要求
- [ ] 导出并校验 OpenAPI 文档，避免实现和契约漂移

### 2. 测试体系

- [ ] 补齐领域状态机和金额计算单元测试
- [ ] 补齐 Controller、Security 和数据库事务集成测试
- [ ] 使用 Testcontainers 验证 PostgreSQL，减少 H2 方言差异
- [ ] 使用 Testcontainers 验证 Redis Lua、RabbitMQ、Elasticsearch 和 MinIO
- [ ] 增加下单、支付、退款和优惠券端到端测试
- [ ] 增加库存、优惠券和幂等接口的并发测试
- [ ] 设定核心模块测试覆盖率门槛

### 3. 压测与性能优化

- [ ] 使用 k6 或 JMeter 编写商品查询、购物车、下单、领券和搜索场景
- [ ] 记录吞吐量、P50/P95/P99、错误率和资源占用基线
- [ ] 观察数据库连接池、慢 SQL、Redis 热 Key、大 Key 和 MQ 积压
- [ ] 对比数据库版与 Redis Lua 领券方案
- [ ] 对比商品缓存、ES 搜索和直接数据库查询的表现
- [ ] 根据数据调整索引、批量大小、连接池和超时，而不是凭经验优化

### 4. 分布式追踪与可观测性闭环

- [ ] 接入 Micrometer Tracing 和 OpenTelemetry
- [ ] 使用 Tempo 保存并查询 Trace
- [ ] 串联 HTTP、数据库、Redis 和 RabbitMQ 的调用链
- [ ] 确保 Trace ID 可以关联指标、日志和消息
- [ ] 为订单、支付、退款、优惠券和索引同步定义 SLI/SLO
- [ ] 完成告警触发、通知、静默和恢复演练

### 5. 容器化与 CI/CD

- [x] 编写多阶段 Dockerfile，并使用非 root 用户运行应用
- [ ] 增加 Docker Compose 应用服务、健康检查和启动依赖
- [x] 使用 GitHub Actions 执行编译、测试和容器构建检查
- [x] 自动构建带分支、版本和 Commit SHA 的应用镜像并发布到 GHCR
- [ ] 在流水线中检查 Flyway Migration 和 OpenAPI 变更
- [x] 镜像不内置 Secret，敏感配置通过部署环境注入
- [ ] 学习 Kubernetes Deployment、Service、ConfigMap、Secret 和滚动发布

### 6. 架构治理与安全

- [ ] 使用 Spring Modulith 或 ArchUnit 约束模块依赖方向
- [ ] 禁止 Controller 绕过 Service 直接访问其他模块 Repository
- [ ] 增加 Checkstyle/Spotless、静态分析和依赖漏洞扫描
- [ ] 检查认证、水平越权、敏感日志、上传安全和管理接口权限
- [ ] 编写数据库备份恢复、消息积压和外部依赖故障演练文档
- [ ] 清理开发默认密码，完善最小权限与审计策略

### 工程化完成标准

```text
接口契约可查、可验证
→ 核心业务有单元/集成/端到端/并发测试
→ 真实中间件由 Testcontainers 验证
→ 关键接口有性能基线
→ 指标、日志、Trace 和告警能够闭环排障
→ 镜像可以自动构建和部署
→ 模块边界、安全与依赖受到自动检查
```

## 可选的生产业务扩展

以下能力属于真实大型电商的长期建设方向，不作为当前学习项目的完成门槛：

- [ ] 多商家、店铺、类目和品牌
- [ ] 配送、运费模板和物流轨迹
- [ ] 售后、退货、换货与退货物流
- [ ] 发票、账务、商家结算和财务对账
- [ ] 真实支付渠道签名、证书轮换和渠道对账
- [ ] 风控、反作弊、验证码和设备识别
- [ ] 数据隐私、脱敏、审计和合规
- [ ] 高可用部署、备份恢复、容量规划与故障演练
- [ ] Kubernetes 生产治理、灰度发布和多环境配置

这些扩展应根据学习目标选择一两个深入实现，而不是全部塞进单体项目。工程化验收完成后，可以优先尝试把搜索或支付拆成独立服务，对比单体本地事务与分布式最终一致性的真实成本。

## 近期执行顺序

```text
数据库版优惠券与状态机
→ 下单锁券、支付核销、取消释放
→ 优惠快照与订单项金额分摊
→ 并发测试与 Redis Lua 预扣
→ RabbitMQ 异步落库
→ 对账、补偿、指标与告警
→ 按订单项退款
→ DLQ 管理与重放
→ OpenAPI 与完整测试体系
→ 压测、Tracing 和 SLO
→ Docker、CI/CD 与架构治理
→ 项目工程化验收
```
