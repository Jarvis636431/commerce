# Commerce 学习路线图

这个项目以模块化单体为主线，目标不是堆砌中间件，而是通过电商业务理解 Java 后端常见的数据、缓存、消息、安全、搜索、存储和可观测性方案。

## 已完成

- 商品、SKU 与库存
- 库存预占、确认、释放和并发控制
- 订单、成交快照与地址快照
- 支付、超时关单和库存补偿
- 用户、地址、Spring Security 与双 Token
- Redis 购物车与商品详情缓存
- RabbitMQ、延迟消息、重试和死信队列
- Transactional Outbox 与至少一次投递
- Prometheus、Loki、Alloy、Grafana 与 Alertmanager
- 整单和部分金额退款
- 退款创建幂等、回调幂等、额度锁定和异步渠道提交

## 下一阶段：搜索与对象存储

### 1. Elasticsearch 商品搜索

目标：不再使用数据库 `LIKE` 承担复杂商品检索。

- [x] 使用 Spring Data Elasticsearch 接入 Elasticsearch
- [x] 设计商品搜索文档和 Index Mapping
- [x] 支持名称、描述、SKU 的全文检索
- [ ] 支持价格区间、上下架状态过滤和更多排序方式
- 学习中文分词、倒排索引、相关性评分和高亮
- [ ] 使用 Outbox/RabbitMQ 可靠同步 PostgreSQL 商品变更到 ES
- [x] 实现事务提交后增量更新和管理员全量重建
- [x] 明确 PostgreSQL 是事实源，ES 是可以重建的查询副本
- 增加索引同步失败指标、日志和告警

### 2. MinIO / S3 对象存储

目标：商品图片不保存到数据库，也不依赖应用服务器本地磁盘。

- 使用 MinIO 在本地模拟 S3 对象存储
- 实现商品图片上传、查询和删除
- 数据库只保存对象 Key、类型、大小和业务归属
- 使用预签名 URL 让客户端直传和下载
- 校验 MIME、扩展名、文件大小和对象 Key
- 学习 Bucket、Object、ETag、生命周期和访问策略
- 处理数据库记录与对象存储之间的一致性和垃圾文件清理
- 后续可无缝迁移到 AWS S3、阿里云 OSS 或腾讯云 COS

推荐顺序是先 Elasticsearch、后 MinIO。搜索可以直接复用商品模块、Outbox 和 MQ，把“数据库事实源 + 异构索引最终一致性”串起来；对象存储随后作为独立且边界清晰的文件能力接入。

## 退款模块后续增强

这些能力已经进入 Roadmap，当前不阻塞搜索和对象存储学习：

### 按订单项退款与金额分摊

- 新增 `RefundItem` 退款明细
- 按订单项成交单价和数量计算退款金额
- 控制累计可退数量不超过购买数量
- 在优惠券模块完成后，根据商品实付金额快照退款
- 处理优惠分摊的四舍五入和最后一项尾差
- 处理运费退款、订单级优惠和多商家分摊

### DLQ 管理与重放

- 将 RabbitMQ 死信持久化为 `DeadLetterRecord`
- 提供管理员分页查询和失败原因查看接口
- 记录处理人、处理备注和审计时间
- 管理员重放时创建新 Outbox 事件，不直接操作 RabbitMQ
- 防止重复重放，并记录原事件与新事件的关联
- 增加 DLQ 深度、持久化失败和重放失败告警

### 可靠性补充

- 扫描长时间停留在 `PROCESSING` 的退款并主动查询渠道
- 人工审核和退款审批
- 退款账务流水与财务对账
- 售后、退货、换货和退货物流

完成订单项退款、金额分摊、DLQ 管理和超时补偿后，可以认为退款模块的核心业务与可靠性闭环较完整；真实资金生产系统仍需要账务、对账、审核和风控。

## 后续候选模块

1. 优惠券与促销：领取限制、规则计算、Redis Lua 和并发核销
2. OpenAPI：完善接口契约、示例、鉴权说明和错误模型
3. 压测与性能优化：使用 k6/JMeter 验证 PostgreSQL、Redis、MQ 和 ES
4. 分布式追踪：OpenTelemetry、Micrometer Tracing 与 Tempo
5. Docker/Kubernetes：容器化应用、健康检查、配置和滚动发布
6. 模块化边界：Spring Modulith 事件与架构测试

## 近期执行顺序

```text
Elasticsearch 基础设施与商品索引
→ 商品搜索接口
→ Outbox 增量同步与全量重建
→ MinIO 基础设施
→ 商品图片元数据与预签名上传
→ 对象清理补偿
→ 回到订单项退款与 DLQ 管理
```
