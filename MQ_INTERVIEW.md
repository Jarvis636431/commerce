# MQ 高频面试题与回答框架

回答 MQ 问题时，建议始终按“发生位置 → 失败场景 → 解决办法 → 方案代价”展开，不要只背名词。

## 1. 为什么使用消息队列

核心价值是异步、削峰、解耦和延迟处理：非核心任务不阻塞请求；瞬时流量先进入 Broker；生产者不依赖具体下游；订单超时等任务可在未来触发。

代价是引入最终一致性、重复或丢失、积压、顺序问题，以及额外的部署、监控和排障成本。需要立即返回结果、适合本地事务的核心链路通常继续同步执行。

## 2. RabbitMQ 的消息模型

Producer 将消息和 Routing Key 发给 Exchange；Exchange 根据 Binding 将消息路由到 Queue；Consumer 从 Queue 获取消息并 ACK。

- direct：Routing Key 完全匹配。
- topic：`*` 匹配一个单词，`#` 匹配零个或多个单词。
- fanout：广播给所有绑定队列。
- headers：根据 Header 条件路由。

Connection 是应用与 Broker 的 TCP 连接，创建成本较高；Channel 是复用 Connection 的轻量逻辑通道，不应为每条消息创建 Connection。

## 3. 如何保证消息不丢失

必须分段回答：

### 数据库到 Producer

数据库提交成功、消息发送前宕机是最难的窗口。可使用 Transactional Outbox：业务数据和事件记录在同一数据库事务提交，再由后台任务可靠投递事件。

### Producer 到 Broker

- Publisher Confirm 处理 Broker 的 ACK/NACK。
- Publisher Return 配合 `mandatory=true` 发现无法路由的消息。
- 发送失败应持久化重试，不能只在内存里重试。

### Broker 内部

Exchange 和 Queue 设置 durable，消息设置 persistent；高可靠队列可使用合适的复制策略，例如 Quorum Queue。持久化会显著降低丢失概率，但不等于绝对不会丢失。

### Broker 到 Consumer

消费者完成业务事务后再 ACK；失败时 NACK/Reject，根据异常类型选择重试或进入死信队列。

## 4. 为什么消息会重复，如何幂等

消费者可能已经提交数据库事务，但在 ACK 前宕机。Broker 不知道业务已经成功，就会重新投递。因此通常按 at-least-once（至少一次）设计。

常见幂等方案：

- 数据库唯一约束，例如外部支付流水号唯一。
- 消费记录表，对 `(consumerName, eventId)` 建立唯一约束。
- 状态机校验，只允许合法的单向状态转换。
- Redis `SET NX` 可用于短期去重，关键业务仍需数据库兜底。

“先查再写”本身不幂等，两个消费者可能同时查到不存在；还必须依赖唯一约束、锁或原子操作抵御并发。

## 5. 如何处理消费失败和重试

先区分异常：网络抖动、数据库短暂不可用属于瞬时异常，可退避重试；消息格式错误、业务数据非法属于永久异常，应进入死信队列。

重试必须有最大次数和退避时间，避免立即无限重入队导致 CPU 空转和日志风暴。超过次数后进入 DLQ，由告警、人工处理或补偿任务接管。重新投递仍然要求幂等。

当前项目第一版处理异常时直接拒绝到 DLQ，并由数据库定时扫描补偿支付超时；后续可增加有限次数的指数退避重试。

## 6. 什么是死信队列

RabbitMQ 中消息可能因为以下情况成为死信：

- 消费者 Reject/NACK 且 `requeue=false`。
- 消息 TTL 到期。
- 队列达到长度限制等策略条件。

DLX 是死信交换机，负责重新路由；DLQ 是最终承接死信的普通队列。死信队列不是垃圾桶，必须配置监控、告警、原因记录和处理流程。

## 7. 如何实现延迟消息

RabbitMQ 常见方案：

- Queue TTL + DLX：消息在无消费者的延迟队列中过期，再路由到消费队列。
- 每消息 TTL + DLX：更加灵活，但经典队列可能发生队首阻塞。
- 延迟消息插件：需要额外安装和维护。
- 应用时间轮或定时任务：适合特定规模与精度要求。

RocketMQ 提供原生延迟消息，更适合大量、多时间点的定时投递。

## 8. 如何保证消息顺序

全局顺序会严重限制并发，通常只保证同一订单等业务键的局部顺序。可按 `orderId` 将消息路由到同一个队列或分区，并串行处理。扩容、失败重试和重新入队仍可能破坏顺序，所以消费者还应使用状态机校验。

## 9. 消息积压怎么办

先观察生产速率、消费速率、未确认消息、单条耗时和失败率，判断瓶颈在 Broker、消费者、数据库还是下游。

- 在下游容量允许时增加消费者实例或并发度。
- 批量拉取和批量写库，减少网络及事务开销。
- 对非核心生产流量限流或降级。
- 必要时临时转储积压消息，再离线恢复消费。

不能盲目增加消费者，否则可能把数据库连接池和下游服务打垮。

## 10. 如何处理过期消息

消息应携带 `occurredAt`、业务截止时间或版本号，消费者执行前判断是否仍有业务价值。过期消息可以 ACK 并记录指标，或者进入审计流程，不能一律执行。

## 11. Push 和 Pull 有什么区别

Push 通常是客户端保持长连接，由 Broker 主动投递，实时性和使用体验好；Pull 是消费者主动按节奏拉取，更便于控制批量和速率。不同客户端可能封装底层拉取行为，回答时应区分 API 使用模型与协议实现。

## 12. 消息事务和数据库事务的关系

Publisher Confirm 只覆盖消息发布，不能和业务数据库自然组成一个 ACID 事务。常见方案包括：

- Outbox：业务表和消息表使用同一本地事务，通用、边界清晰。
- RocketMQ 事务消息：先发 half message，再执行本地事务并提交或回滚；状态未知时 Broker 回查。
- 最大努力通知：适合可查询、可补偿并允许人工介入的外围业务。

不管使用哪种方式，下游消费仍可能重复，消费者幂等不能省略。

## 13. RabbitMQ、RocketMQ、Kafka 如何选

- RabbitMQ：复杂路由、业务任务、易观察，以及成熟的 AMQP/Spring 生态。
- RocketMQ：高吞吐业务消息、大量堆积、原生延迟消息和事务消息。
- Kafka：事件流、日志、数据管道、长时间保留、高吞吐与消息回放。

选型要结合消息量、堆积规模、顺序和延迟要求、团队经验、运维能力及云服务支持，不能只看吞吐量排名。

## 14. RabbitMQ 如何实现高可用

生产环境通常部署多节点，根据可靠性要求选择复制队列策略。Quorum Queue 使用多数派复制，更适合重要的持久业务消息。客户端还应配置多个地址、自动恢复、合理超时和发布确认。

高可用不等于无故障，网络分区、磁盘满和消费者全部不可用仍需要监控及应急预案。

## 15. 如何设计业务消息

不要直接发送 JPA Entity，应定义稳定的事件契约：

```json
{
  "eventId": "唯一事件 ID",
  "eventType": "payment.timeout.scheduled",
  "aggregateId": "PAY123",
  "occurredAt": "2026-09-03T08:00:00Z",
  "schemaVersion": 1,
  "payload": {}
}
```

重要字段包括事件 ID、事件类型、业务实体 ID、发生时间和协议版本。消息只携带必要信息，消费者可按 ID 查询最新状态，避免依赖已经过期的大对象快照。

## 16. 当前项目的回答示例

问：项目如何实现支付超时关单？

答：创建支付单的数据库事务提交后，应用向 RabbitMQ direct exchange 发送包含 `eventId`、`paymentNo` 和 `expiresAt` 的持久消息，并通过 Publisher Confirm 和 Return 观察 Broker 接收及路由结果。消息进入 TTL 延迟队列，15 分钟后通过 DLX 路由到超时队列。消费者重新查询支付状态；只有仍为 PENDING 且已到期时，才在数据库事务中关闭支付、取消订单并释放预占库存。方法成功返回后由容器 ACK，异常会进行三次退避重试，耗尽后拒绝到 DLQ。重复或提前到达的消息会被安全忽略，同时保留数据库定时扫描作为补偿。当前 `afterCommit + 扫描补偿` 仍有及时性窗口，后续可使用 Outbox 实现数据库与事件投递的可靠衔接。

这个回答覆盖了延迟消息、事务边界、幂等、ACK、死信和补偿，比只说“使用了 RabbitMQ”更完整。
